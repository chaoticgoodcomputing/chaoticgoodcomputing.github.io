#!/bin/bash
# Post-push hook: Update submodule reference after pushing if it changed

# Unset git environment variables
unset GIT_DIR
unset GIT_WORK_TREE
unset GIT_INDEX_FILE

SUBMODULE_PATH="content/private"

# Check if submodule reference has changed
if [ -d "$SUBMODULE_PATH" ] && [ -e "$SUBMODULE_PATH/.git" ]; then
  # Check if the submodule reference differs from what's committed
  if ! git diff --quiet -- "$SUBMODULE_PATH" 2>/dev/null; then
    
    echo ""
    echo "📌 Updating submodule reference after push..."
    
    # Submodule reference differs from what's committed
    git add "$SUBMODULE_PATH" 2>/dev/null
    
    if git commit -m "Update private submodule reference" 2>/dev/null; then
      git push 2>/dev/null && \
      echo "✓ Submodule reference updated and pushed"
    fi
  fi
fi

exit 0
